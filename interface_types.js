"use strict";
function print(coord) { }
const c2 = (point) => {
    const d2 = point; //В этом случае point сконвертируется в I3DPoint и будет добавлено свойство z.
};
const myCanvas = document.getElementById('canvas');
//type Test = { - для типов не будет работать.
//    b : number
//}
